card = input('Card Number? ')
var = 0

for i in range(1, length(card), 2):
    value = int(card(i)) * 2
    if value > 9:
        value = str(value)
        value = int(value[1]) + int(value([2])
    
    var += value


print(f"{round_1_sum}")